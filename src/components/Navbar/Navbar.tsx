import { Logo } from "../../assets/imgs";
import { Container } from "./styles";

export const Navbar = () => {
    return (
        <Container>
            <img src={Logo} />
        </Container>
    );
};
